# PropertyFinderGUI
Property finder website. A GUI that communicates with a database to process queries.


### Use the dump.sql file that contains the SQL tables and data. 
### Upload it to any database hosting service, and change the credentials in each query file.

